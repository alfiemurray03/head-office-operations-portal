{
  "name": "head-office-operations-portal",
  "version": "7.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler pages functions build --outdir .pages-check",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "types": "wrangler types --path functions/types.d.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "wrangler": "^4.0.0"
  }
}
