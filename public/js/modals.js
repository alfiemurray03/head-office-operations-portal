function modalForm(title, description, body, submitLabel, eyebrow = 'Head Office record', submitClass = 'primary') {
  openModal(title, description, `<form data-form="${body.form}" ${body.attributes || ''}>${body.html}<p class="form-error"></p><div class="form-actions"><button type="button" class="button secondary" data-close-modal>Cancel</button><button class="button ${submitClass}">${escapeHtml(submitLabel)}</button></div></form>`, eyebrow);
}

function newCustomerModal() {
  modalForm('Register universal customer', 'Create the authoritative company-wide customer identity record.', { form: 'new-customer', html: `<div class="form-grid"><label class="field full"><span>Full name</span><input name="displayName" maxlength="160" required></label><label class="field"><span>Verified email address</span><input name="verifiedEmail" type="email" maxlength="254" required></label><label class="field"><span>Mobile number (optional)</span><input name="mobile" maxlength="40"></label><label class="field"><span>External identity ID (optional)</span><input name="externalIdentityId" maxlength="100"></label><label class="field"><span>Originating division (optional)</span><select name="originatingPlatformId">${options(state.reference.platforms, 'id', 'name', '', 'None / Head Office')}</select></label></div>` }, 'Register customer', 'Universal Customer Register');
}

function newCaseModal(forcedType = '') {
  const caseTypes = ['general','security','complaint','refund','payment_dispute','account_recovery',...(hasPermission('data_protection:*') ? ['data_protection'] : []),...(hasPermission('safeguarding:*') ? ['safeguarding'] : [])];
  modalForm('Create Head Office case', 'Open a controlled workflow with a formal reference, priority and due time.', { form: 'new-case', html: `<div class="form-grid"><label class="field"><span>Case type</span><select name="caseType" required>${caseTypes.map(value => `<option value="${value}" ${forcedType === value ? 'selected' : ''}>${label(value)}</option>`).join('')}</select></label><label class="field"><span>Priority</span><select name="priority"><option value="normal">Normal</option><option value="high">High</option><option value="critical">Critical</option><option value="low">Low</option></select></label><label class="field"><span>Universal customer number (optional)</span><input name="customerNumber" maxlength="10" inputmode="numeric"></label><label class="field"><span>Originating division (optional)</span><select name="platformId">${options(state.reference.platforms, 'id', 'name', '', 'None / Head Office')}</select></label><label class="field full"><span>Case title</span><input name="title" maxlength="160" required></label><label class="field full"><span>Summary and reason</span><textarea name="description" maxlength="4000" required></textarea></label><label class="field"><span>Due date (optional)</span><input name="dueAt" type="datetime-local"></label></div>` }, 'Create case', 'Head Office workflow');
}

function newMarkerModal(customerNumber = '', caseReference = '') {
  modalForm('Apply security marker', 'Record confidential risk information and the staff visibility permitted for it.', { form: 'new-marker', html: `<div class="form-grid"><label class="field"><span>Universal customer number</span><input name="customerNumber" value="${escapeHtml(customerNumber)}" maxlength="10" required></label><label class="field"><span>Linked case reference</span><input name="caseReference" value="${escapeHtml(caseReference)}" maxlength="30" required></label><label class="field"><span>Marker type</span><select name="markerType" required>${options(state.reference.markerTypes, 'code', 'label')}</select></label><label class="field"><span>Risk level override</span><select name="riskLevel"><option value="">Use catalogue default</option>${['low','moderate','high','critical'].map(value => `<option value="${value}">${label(value)}</option>`).join('')}</select></label><label class="field"><span>Visibility override</span><select name="visibility"><option value="">Use catalogue default</option>${['head_office_only','branch_instruction','approved_branch_summary','system_enforced'].map(value => `<option value="${value}">${label(value)}</option>`).join('')}</select></label><label class="field"><span>Review date (optional)</span><input name="reviewAt" type="datetime-local"></label><label class="field full"><span>Confidential reason</span><textarea name="reason" maxlength="2000" required></textarea></label></div>` }, 'Apply marker', 'Security Control Centre');
}

function newRestrictionModal(customerNumber = '', caseReference = '') {
  modalForm('Apply customer restriction', 'Create an enforceable control for all divisions or one selected platform.', { form: 'new-restriction', html: `<div class="form-grid"><label class="field"><span>Universal customer number</span><input name="customerNumber" value="${escapeHtml(customerNumber)}" maxlength="10" required></label><label class="field"><span>Linked case reference</span><input name="caseReference" value="${escapeHtml(caseReference)}" maxlength="30"></label><label class="field"><span>Restriction type</span><select name="restrictionType" required>${options(state.reference.restrictionTypes, 'code', 'label')}</select></label><label class="field"><span>Scope</span><select name="scope"><option value="company_wide">Company wide</option>${state.reference.platforms.map(platform => `<option value="${escapeHtml(platform.id)}">${escapeHtml(platform.name)}</option>`).join('')}</select></label><label class="field"><span>Review date</span><input name="reviewAt" type="datetime-local"></label><label class="field"><span>Expiry date (optional)</span><input name="expiresAt" type="datetime-local"></label><label class="field full"><span>Confidential reason</span><textarea name="reason" maxlength="2000" required></textarea></label></div>` }, 'Apply restriction', 'Security Control Centre', 'danger');
}

function newCommunicationModal(customerNumber = '', caseReference = '') {
  modalForm('Log communication', 'Add an auditable customer contact entry to the company-wide history.', { form: 'new-communication', html: `<div class="form-grid"><label class="field"><span>Universal customer number</span><input name="customerNumber" value="${escapeHtml(customerNumber)}" maxlength="10"></label><label class="field"><span>Case reference (optional)</span><input name="caseReference" value="${escapeHtml(caseReference)}" maxlength="30"></label><label class="field"><span>Direction</span><select name="direction"><option value="inbound">Inbound</option><option value="outbound">Outbound</option><option value="internal">Internal</option></select></label><label class="field"><span>Channel</span><select name="channel"><option value="email">Email</option><option value="telephone">Telephone</option><option value="whatsapp">WhatsApp</option><option value="letter">Letter</option><option value="web_form">Web form</option><option value="system">System</option></select></label><label class="field full"><span>Subject (optional)</span><input name="subject" maxlength="200"></label><label class="field full"><span>Contact summary</span><textarea name="summary" maxlength="4000" required></textarea></label><label class="field"><span>Date and time</span><input name="occurredAt" type="datetime-local" value="${formatDateInput(new Date())}"></label></div>` }, 'Record communication', 'Customer communications');
}

function newPaymentModal() {
  modalForm('Record payment or refund', 'Store the provider reference and create a formal approval request where required.', { form: 'new-payment', html: `<div class="form-grid"><label class="field"><span>Universal customer number (optional)</span><input name="customerNumber" maxlength="10"></label><label class="field"><span>Linked case reference</span><input name="caseReference" maxlength="30"></label><label class="field"><span>Provider</span><input name="provider" value="Stripe" maxlength="80" required></label><label class="field"><span>Provider payment reference</span><input name="providerPaymentReference" maxlength="200" required></label><label class="field"><span>Provider customer reference</span><input name="providerCustomerReference" maxlength="200"></label><label class="field"><span>Division</span><select name="platformId">${options(state.reference.platforms, 'id', 'name', '', 'Head Office / none')}</select></label><label class="field"><span>Amount</span><input name="amount" type="number" min="0" step="0.01" required></label><label class="field"><span>Currency</span><input name="currency" value="GBP" maxlength="3" required></label><label class="field"><span>Status</span><select name="status"><option value="captured">Captured</option><option value="pending">Pending</option><option value="authorised">Authorised</option><option value="failed">Failed</option><option value="refund_requested">Refund requested</option><option value="refunded">Refunded</option><option value="disputed">Disputed</option><option value="cancelled">Cancelled</option></select></label><label class="field"><span>Date and time</span><input name="occurredAt" type="datetime-local" value="${formatDateInput(new Date())}"></label></div>` }, 'Record reference', 'Payments & approvals');
}

async function openCustomer(id) {
  const data = await api(`/api/customers/${encodeURIComponent(id)}`);
  const customer = data.customer;
  const caseRows = data.cases.length ? data.cases.map(record => `<tr data-open="case" data-id="${record.id}"><td class="mono">${escapeHtml(record.case_reference)}</td><td>${escapeHtml(record.title)}</td><td>${tag(record.case_type)}</td><td>${tag(record.status)}</td></tr>`).join('') : '<tr><td colspan="4">No linked cases.</td></tr>';
  const markerRows = data.markers.length ? data.markers.map(item => `<div class="summary-item"><span>${escapeHtml(item.marker_type)}</span><strong>${escapeHtml(item.marker_label || label(item.marker_type))} ${tag(item.risk_level)}</strong><small>${escapeHtml(item.reason)} · ${tag(item.status)}</small></div>`).join('') : '<p class="help-text">No security markers visible to your role.</p>';
  const restrictionRows = data.restrictions.length ? data.restrictions.map(item => `<div class="summary-item"><span>${escapeHtml(item.enforcement_action || item.restriction_type)}</span><strong>${escapeHtml(item.restriction_label || label(item.restriction_type))} ${tag(item.status)}</strong><small>${escapeHtml(label(item.scope))}</small></div>`).join('') : '<p class="help-text">No restrictions visible to your role.</p>';
  const editor = hasPermission('customers:write') ? `<form data-form="update-customer" data-id="${customer.id}" class="form-grid"><label class="field"><span>Customer name</span><input name="displayName" value="${escapeHtml(customer.display_name)}" required></label><label class="field"><span>Verified email</span><input name="verifiedEmail" type="email" value="${escapeHtml(customer.verified_email)}" required></label><label class="field"><span>Account status</span><select name="accountStatus">${['pending','active','restricted','suspended','closed','archived'].map(value => `<option value="${value}" ${customer.account_status === value ? 'selected' : ''}>${label(value)}</option>`).join('')}</select></label>${hasPermission('security:write') ? `<label class="field"><span>Security status</span><select name="securityStatus">${['clear','monitor','review','high','critical'].map(value => `<option value="${value}" ${customer.security_status === value ? 'selected' : ''}>${label(value)}</option>`).join('')}</select></label>` : ''}<div class="form-actions full"><button class="button primary">Save customer record</button></div><p class="form-error full"></p></form>` : '';
  openModal(customer.display_name, `Universal customer ${customer.customer_number}`, `<div class="detail-grid"><aside class="summary-list"><div class="summary-item"><span>Universal number</span><strong class="mono">${escapeHtml(customer.customer_number)}</strong></div><div class="summary-item"><span>Verified email</span><strong>${escapeHtml(customer.verified_email)}</strong></div><div class="summary-item"><span>Account status</span><strong>${tag(customer.account_status)}</strong></div><div class="summary-item"><span>Security status</span><strong>${tag(customer.security_status)}</strong></div><div class="summary-item"><span>First registered</span><strong>${formatDate(customer.first_registered_at)}</strong></div></aside><div><div class="heading-actions" style="margin-bottom:15px">${hasPermission('cases:create') ? `<button class="button secondary small" data-action="new-case-for-customer" data-customer="${customer.customer_number}">＋ Case</button>` : ''}${hasPermission('communications:write') ? `<button class="button secondary small" data-action="new-communication-for-customer" data-customer="${customer.customer_number}">＋ Contact</button>` : ''}${hasPermission('security:write') ? `<button class="button secondary small" data-action="new-marker-for-customer" data-customer="${customer.customer_number}">＋ Marker</button><button class="button danger small" data-action="new-restriction-for-customer" data-customer="${customer.customer_number}">＋ Restriction</button>` : ''}</div>${editor}<h3>Linked cases</h3><div class="table-wrap"><table class="data-table"><thead><tr><th>Reference</th><th>Case</th><th>Type</th><th>Status</th></tr></thead><tbody>${caseRows}</tbody></table></div>${hasPermission('security:read') ? `<div class="split-grid" style="margin-top:18px"><div><h3>Security markers</h3><div class="summary-list">${markerRows}</div></div><div><h3>Restrictions</h3><div class="summary-list">${restrictionRows}</div></div></div>` : ''}</div></div>`, 'Universal Customer Register');
}

async function openCase(id) {
  const data = await api(`/api/cases/${encodeURIComponent(id)}`);
  const record = data.case;
  const notes = data.notes.length ? data.notes.map(note => `<div class="timeline-item"><strong>${escapeHtml(note.created_by_name || 'Staff member')} · ${escapeHtml(label(note.note_type))}</strong><p>${escapeHtml(note.body)}</p><time>${escapeHtml(label(note.visibility))} · ${formatDate(note.created_at)}</time></div>`).join('') : emptyState('No case notes', 'Add the first controlled case note.');
  const communications = data.communications.length ? data.communications.map(item => `<div class="timeline-item"><strong>${escapeHtml(label(item.direction))} ${escapeHtml(label(item.channel))}${item.subject ? ` · ${escapeHtml(item.subject)}` : ''}</strong><p>${escapeHtml(item.summary)}</p><time>${formatDate(item.occurred_at)}</time></div>`).join('') : '<p class="help-text">No communications linked to this case.</p>';
  openModal(record.case_reference, record.title, `<div class="detail-grid"><aside class="summary-list"><div class="summary-item"><span>Case type</span><strong>${tag(record.case_type)}</strong></div><div class="summary-item"><span>Customer</span><strong>${escapeHtml(record.customer_name || 'No linked customer')}</strong><small class="mono">${escapeHtml(record.customer_number || '')}</small></div><div class="summary-item"><span>Status</span><strong>${tag(record.status)}</strong></div><div class="summary-item"><span>Priority</span><strong>${tag(record.priority)}</strong></div><div class="summary-item"><span>Due</span><strong>${formatDate(record.due_at)}</strong></div><div class="summary-item"><span>Opened</span><strong>${formatDate(record.opened_at)}</strong></div></aside><div><div class="notice"><span>▣</span><div><strong>Case summary</strong><br>${escapeHtml(record.description)}</div></div><form data-form="update-case" data-id="${record.id}" class="form-grid"><label class="field"><span>Status</span><select name="status">${['open','triage','investigating','awaiting_customer','awaiting_internal','approval_required','resolved','closed','cancelled'].map(value => `<option value="${value}" ${record.status === value ? 'selected' : ''}>${label(value)}</option>`).join('')}</select></label><label class="field"><span>Priority</span><select name="priority">${['critical','high','normal','low'].map(value => `<option value="${value}" ${record.priority === value ? 'selected' : ''}>${label(value)}</option>`).join('')}</select></label><label class="field"><span>Assigned staff</span><select name="assignedStaffId">${options(state.reference.staff, 'id', 'display_name', record.assigned_staff_id, 'Unassigned')}</select></label><label class="field"><span>Due date</span><input name="dueAt" type="datetime-local" value="${formatDateInput(record.due_at)}"></label><div class="form-actions full"><button class="button primary">Update case</button></div><p class="form-error full"></p></form><div class="heading-actions" style="margin:18px 0">${hasPermission('communications:write') ? `<button class="button secondary small" data-action="new-communication-for-case" data-case="${record.case_reference}" data-customer="${escapeHtml(record.customer_number || '')}">＋ Communication</button>` : ''}${hasPermission('security:write') && record.customer_number ? `<button class="button secondary small" data-action="new-marker-for-case" data-case="${record.case_reference}" data-customer="${record.customer_number}">＋ Marker</button><button class="button danger small" data-action="new-restriction-for-case" data-case="${record.case_reference}" data-customer="${record.customer_number}">＋ Restriction</button>` : ''}</div><h3>Case notes</h3><div class="timeline">${notes}</div><form data-form="case-note" data-id="${record.id}" class="form-grid" style="margin-top:16px"><label class="field"><span>Note type</span><select name="noteType"><option value="internal">Internal</option><option value="customer_contact">Customer contact</option><option value="decision">Decision</option></select></label><label class="field"><span>Visibility</span><select name="visibility"><option value="case_team">Case team</option><option value="head_office">Head Office</option>${hasPermission('data_protection:*') ? '<option value="restricted_dpo">Restricted DPO</option>' : ''}${hasPermission('safeguarding:*') ? '<option value="restricted_safeguarding">Restricted safeguarding</option>' : ''}</select></label><label class="field full"><span>New case note</span><textarea name="body" required></textarea></label><div class="form-actions full"><button class="button secondary">Add note</button></div><p class="form-error full"></p></form><h3 style="margin-top:22px">Linked communications</h3><div class="timeline">${communications}</div></div></div>`, label(record.case_type));
}

function actionConfirmation(title, description, action, id, buttonLabel, danger = false) {
  const approval = action.startsWith('approval-');
  openModal(title, description, `<form data-form="record-action" data-action-name="${escapeHtml(action)}" data-id="${escapeHtml(id)}"><label class="field"><span>${action.includes('review') ? 'Next review date' : 'Decision reason'}</span><input name="value" ${action.includes('review') ? 'type="datetime-local"' : 'placeholder="Record the reason"'} ${approval ? 'required minlength="3"' : ''}></label><p class="form-error"></p><div class="form-actions"><button type="button" class="button secondary" data-close-modal>Cancel</button><button class="button ${danger ? 'danger' : 'primary'}">${escapeHtml(buttonLabel)}</button></div></form>`);
}
