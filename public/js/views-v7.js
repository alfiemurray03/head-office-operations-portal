function riskScore(score = 0) {
  const value = Math.max(0, Math.min(100, Number(score || 0)));
  return `<div class="risk-score"><strong>${value}</strong><span><i style="width:${value}%"></i></span></div>`;
}

function levelChip(code) {
  const value = String(code || "");
  return `<span class="level-chip ${escapeHtml(value.toLowerCase().replaceAll('-',''))}">${escapeHtml(value)}</span>`;
}

async function renderControlRoomV7() {
  const data = await api('/api/v7/overview');
  const m = data.metrics || {};
  const alertRows = data.alerts.length ? data.alerts.map(item => `<tr><td class="mono">${escapeHtml(item.alert_reference)}</td><td><strong>${escapeHtml(item.title)}</strong><br><small>${escapeHtml(item.customer_name || item.customer_number || 'No linked customer')}</small></td><td>${riskScore(item.risk_score)}</td><td>${levelChip(item.risk_level)} ${levelChip(item.enforcement_level)}</td><td>${levelChip(item.severity)}</td><td>${tag(item.status)}</td></tr>`).join('') : `<tr><td colspan="6">${emptyState('No active alerts','The risk engine has not raised any active alerts.')}</td></tr>`;
  const incidentRows = data.incidents.length ? data.incidents.map(item => `<tr><td class="mono">${escapeHtml(item.incident_reference)}</td><td><strong>${escapeHtml(item.title)}</strong><br><small>${escapeHtml(label(item.category))}</small></td><td>${levelChip(item.severity)}</td><td>${tag(item.status)}</td><td>${tag(item.data_breach_status)}</td><td class="${item.ico_deadline_at && new Date(item.ico_deadline_at) < new Date() ? 'overdue-text' : ''}">${formatDate(item.ico_deadline_at)}</td></tr>`).join('') : `<tr><td colspan="6">${emptyState('No open incidents','No cyber, fraud or personal-data incident currently requires response.')}</td></tr>`;
  const eventRows = data.events.length ? data.events.map(item => `<tr><td>${formatDate(item.received_at)}</td><td class="mono">${escapeHtml(item.event_reference)}</td><td>${escapeHtml(item.event_type)}</td><td>${escapeHtml(item.customer_name || item.customer_number || item.platform_name || 'System')}</td><td>${riskScore(item.risk_score)}</td><td>${levelChip(item.risk_level)} ${levelChip(item.enforcement_level)}</td></tr>`).join('') : `<tr><td colspan="6">${emptyState('No security events','Connected divisions have not submitted any security telemetry yet.')}</td></tr>`;
  $('#viewRoot').innerHTML = `<div class="page-heading"><div><p class="eyebrow">Version ${escapeHtml(data.version)}</p><h1>Head Office Control Room</h1><p>One operational picture across fraud, cyber security, data breaches, complaints, refunds, disputes and general customer operations.</p></div><div class="heading-actions"><button class="button secondary" data-route="security-levels">Security levels</button>${hasPermission('risk:write') ? '<button class="button primary" data-action="new-security-event">＋ Record event</button>' : ''}</div></div>
  <div class="metrics v7-metrics"><article class="metric-card"><div class="metric-icon blue">⌁</div><div><span>Events in 24 hours</span><strong>${Number(m.events24h || 0)}</strong><small>Authentication, payments and system activity</small></div></article><article class="metric-card"><div class="metric-icon amber">!</div><div><span>Open alerts</span><strong>${Number(m.openAlerts || 0)}</strong><small>${Number(m.criticalAlerts || 0)} critical</small></div></article><article class="metric-card"><div class="metric-icon red">◆</div><div><span>Open incidents</span><strong>${Number(m.openIncidents || 0)}</strong><small>${Number(m.breachAssessments || 0)} breach assessments</small></div></article><article class="metric-card"><div class="metric-icon blue">✓</div><div><span>Head Office tasks</span><strong>${Number(m.openTasks || 0)}</strong><small>${Number(m.openComplaints || 0)} complaints · ${Number(m.openFinancialCases || 0)} financial cases</small></div></article></div>
  <div class="split-grid"><section class="panel"><div class="panel-header"><div><h2>Priority risk alerts</h2><p>Explainable signals requiring Head Office review.</p></div><button class="button secondary small" data-route="risk-intelligence">Open intelligence</button></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Alert</th><th>Reason</th><th>Score</th><th>Control</th><th>Severity</th><th>Status</th></tr></thead><tbody>${alertRows}</tbody></table></div></section><section class="panel"><div class="panel-header"><div><h2>Incident response</h2><p>Containment, investigation, recovery and breach deadlines.</p></div><button class="button secondary small" data-route="incidents-v7">Open incidents</button></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Incident</th><th>Summary</th><th>Severity</th><th>Status</th><th>Breach</th><th>ICO deadline</th></tr></thead><tbody>${incidentRows}</tbody></table></div></section></div>
  <section class="panel" style="margin-top:16px"><div class="panel-header"><div><h2>Latest event stream</h2><p>Normalised telemetry received from staff, divisions and approved providers.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Received</th><th>Event</th><th>Type</th><th>Subject</th><th>Score</th><th>Decision</th></tr></thead><tbody>${eventRows}</tbody></table></div></section>`;
}

async function renderRiskIntelligenceV7() {
  const [eventData, alertData] = await Promise.all([api('/api/v7/events'),api('/api/v7/alerts')]);
  const alerts = alertData.alerts.length ? alertData.alerts.map(item => `<tr><td class="mono">${escapeHtml(item.alert_reference)}</td><td><strong>${escapeHtml(item.title)}</strong><br><small>${escapeHtml(item.summary)}</small></td><td>${escapeHtml(item.customer_name || item.customer_number || item.platform_name || 'System')}</td><td>${riskScore(item.risk_score)}</td><td>${levelChip(item.risk_level)} ${levelChip(item.enforcement_level)} ${levelChip(item.severity)}</td><td>${tag(item.status)}</td><td>${hasPermission('risk:write') ? `<button class="button secondary small" data-action="review-v7-alert" data-id="${escapeHtml(item.id)}" data-reference="${escapeHtml(item.alert_reference)}" data-status="${escapeHtml(item.status)}">Review</button>` : ''}</td></tr>`).join('') : `<tr><td colspan="7">${emptyState('No alerts','No events have exceeded the configured review threshold.')}</td></tr>`;
  const events = eventData.events.length ? eventData.events.map(item => `<tr><td>${formatDate(item.received_at)}</td><td class="mono">${escapeHtml(item.event_reference)}</td><td><strong>${escapeHtml(item.event_type)}</strong><br><small>${escapeHtml(label(item.category))}</small></td><td>${escapeHtml(item.customer_name || item.customer_number || item.platform_name || item.source_type)}</td><td>${item.amount_minor == null ? '—' : formatMoney(item.amount_minor,item.currency || 'GBP')}</td><td>${riskScore(item.risk_score)}</td><td>${levelChip(item.risk_level)} ${levelChip(item.enforcement_level)}</td></tr>`).join('') : `<tr><td colspan="7">${emptyState('No event telemetry','Generate a test event or connect a division using the events:write scope.')}</td></tr>`;
  $('#viewRoot').innerHTML = `<div class="page-heading"><div><p class="eyebrow">Detection and fraud intelligence</p><h1>Risk intelligence</h1><p>Correlated authentication, identity, account, payment, refund, dispute and data-security signals.</p></div>${hasPermission('risk:write') ? '<button class="button primary" data-action="new-security-event">＋ Record event</button>' : ''}</div><div class="notice"><span>◎</span><div><strong>Human-controlled decisions</strong><br>Scores explain why a review is required. Protective actions remain recorded, proportionate and reversible.</div></div>
  <section class="panel"><div class="panel-header"><div><h2>Active alerts</h2><p>Signals grouped for triage and investigation.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Alert</th><th>Reason</th><th>Subject</th><th>Score</th><th>Levels</th><th>Status</th><th></th></tr></thead><tbody>${alerts}</tbody></table></div></section>
  <section class="panel" style="margin-top:16px"><div class="panel-header"><div><h2>Security event ledger</h2><p>Immutable event evidence and the resulting risk decision.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Received</th><th>Reference</th><th>Event</th><th>Subject</th><th>Amount</th><th>Score</th><th>Decision</th></tr></thead><tbody>${events}</tbody></table></div></section>`;
}

async function renderIncidentsV7() {
  const data = await api('/api/v7/incidents');
  const rows = data.incidents.length ? data.incidents.map(item => `<tr><td class="mono">${escapeHtml(item.incident_reference)}</td><td><strong>${escapeHtml(item.title)}</strong><br><small>${escapeHtml(item.customer_name || item.customer_number || label(item.category))}</small></td><td>${levelChip(item.severity)} ${levelChip(item.data_classification)} ${levelChip(item.confidentiality_level)}</td><td>${tag(item.status)}</td><td>${tag(item.data_breach_status)}</td><td class="${item.ico_deadline_at && new Date(item.ico_deadline_at)<new Date() && !['reported','not_reportable','not_a_breach'].includes(item.data_breach_status) ? 'overdue-text' : ''}">${formatDate(item.ico_deadline_at)}</td><td>${hasPermission('incidents:write') ? `<button class="button secondary small" data-action="update-v7-incident" data-id="${escapeHtml(item.id)}" data-reference="${escapeHtml(item.incident_reference)}" data-status="${escapeHtml(item.status)}" data-severity="${escapeHtml(item.severity)}" data-breach="${escapeHtml(item.data_breach_status)}">Update</button>` : ''}</td></tr>`).join('') : `<tr><td colspan="7">${emptyState('No incidents','No open cyber, fraud, security or personal-data incidents.')}</td></tr>`;
  $('#viewRoot').innerHTML = `<div class="page-heading"><div><p class="eyebrow">Detect · Respond · Recover</p><h1>Incidents & data breaches</h1><p>Central coordination from discovery and containment through remediation, recovery, reporting and lessons learned.</p></div>${hasPermission('incidents:write') ? '<button class="button danger" data-action="new-v7-incident">＋ Open incident</button>' : ''}</div><div class="notice danger"><span>72h</span><div><strong>Personal-data breach clock</strong><br>The deadline is a decision aid, not an automatic legal conclusion. The DPO must assess reportability and record the rationale.</div></div><section class="panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>Incident</th><th>Summary</th><th>Classification</th><th>Status</th><th>Breach assessment</th><th>ICO deadline</th><th></th></tr></thead><tbody>${rows}</tbody></table></div></section>`;
}

async function renderCentralOperationsV7() {
  const data = await api('/api/v7/operations');
  const taskRows = data.tasks.length ? data.tasks.map(item => `<tr><td class="mono">${escapeHtml(item.task_reference)}</td><td><strong>${escapeHtml(item.title)}</strong><br><small>${escapeHtml(label(item.service_area))} · ${escapeHtml(label(item.task_type))}</small></td><td>${escapeHtml(item.customer_name || item.customer_number || 'No linked customer')}</td><td>${tag(item.priority)}</td><td>${tag(item.status)}</td><td class="${item.due_at && new Date(item.due_at)<new Date() ? 'overdue-text' : ''}">${formatDate(item.due_at)}</td><td>${hasPermission('operations:write') ? `<button class="button secondary small" data-action="update-v7-task" data-id="${escapeHtml(item.id)}" data-reference="${escapeHtml(item.task_reference)}" data-status="${escapeHtml(item.status)}">Update</button>` : ''}</td></tr>`).join('') : `<tr><td colspan="7">${emptyState('No outstanding Head Office tasks','Create a task or open a complaint, refund or dispute case.')}</td></tr>`;
  const complaintRows = data.complaints.length ? data.complaints.map(item => `<tr data-open="case" data-id="${escapeHtml(item.id)}"><td class="mono">${escapeHtml(item.case_reference)}</td><td><strong>${escapeHtml(item.title)}</strong><br><small>${escapeHtml(item.customer_name || item.customer_number || 'No customer')}</small></td><td>${escapeHtml(label(item.complaint_stage || 'received'))}</td><td>${tag(item.priority)}</td><td>${tag(item.status)}</td><td>${formatDate(item.final_response_due_at || item.due_at)}</td></tr>`).join('') : `<tr><td colspan="6">${emptyState('No active complaints','New complaint cases will appear in this central queue.')}</td></tr>`;
  const financialRows = data.financial.length ? data.financial.map(item => `<tr data-open="case" data-id="${escapeHtml(item.id)}"><td class="mono">${escapeHtml(item.case_reference)}</td><td><strong>${escapeHtml(item.title)}</strong><br><small>${escapeHtml(item.customer_name || item.customer_number || 'No customer')}</small></td><td>${escapeHtml(label(item.operation_type || item.case_type))}</td><td>${item.amount_minor == null ? '—' : formatMoney(item.amount_minor,item.currency || 'GBP')}</td><td>${item.fraud_suspected ? levelChip('R3') : '—'}</td><td>${tag(item.status)}</td><td>${formatDate(item.due_at)}</td></tr>`).join('') : `<tr><td colspan="7">${emptyState('No active refunds or disputes','Refund and payment dispute cases will appear here.')}</td></tr>`;
  $('#viewRoot').innerHTML = `<div class="page-heading"><div><p class="eyebrow">Central customer operations</p><h1>Head Office operations</h1><p>One queue for complaints, refunds, payment disputes, customer actions and cross-division follow-up.</p></div><div class="heading-actions">${hasPermission('cases:create') ? '<button class="button secondary" data-action="new-case" data-case-type="complaint">＋ Complaint</button><button class="button secondary" data-action="new-case" data-case-type="refund">＋ Refund case</button><button class="button secondary" data-action="new-case" data-case-type="payment_dispute">＋ Dispute</button>' : ''}${hasPermission('operations:write') ? '<button class="button primary" data-action="new-v7-task">＋ Head Office task</button>' : ''}</div></div>
  <section class="panel"><div class="panel-header"><div><h2>Head Office work queue</h2><p>Assigned actions and deadlines across all operating functions.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Task</th><th>Work</th><th>Customer</th><th>Priority</th><th>Status</th><th>Due</th><th></th></tr></thead><tbody>${taskRows}</tbody></table></div></section>
  <div class="split-grid" style="margin-top:16px"><section class="panel"><div class="panel-header"><div><h2>Complaints</h2><p>Receipt, acknowledgement, investigation, remedy and final response.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Case</th><th>Complaint</th><th>Stage</th><th>Priority</th><th>Status</th><th>Response due</th></tr></thead><tbody>${complaintRows}</tbody></table></div></section><section class="panel"><div class="panel-header"><div><h2>Refunds & disputes</h2><p>Provider evidence, fraud review, approval and outcome.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Case</th><th>Customer</th><th>Type</th><th>Amount</th><th>Risk</th><th>Status</th><th>Due</th></tr></thead><tbody>${financialRows}</tbody></table></div></section></div>`;
}

async function renderSecurityLevelsV7() {
  const data = await api('/api/v7/security-levels');
  const dimensions = ['risk','enforcement','data','authority','confidentiality','severity'];
  const cards = dimensions.map(dimension => `<section class="panel level-panel"><div class="panel-header"><div><p class="eyebrow">${escapeHtml(dimension)}</p><h2>${escapeHtml(label(dimension))}</h2></div></div><div class="level-list">${data.levels.filter(item => item.dimension===dimension).map(item => `<article><div>${levelChip(item.code)}<strong>${escapeHtml(item.label)}</strong></div><p>${escapeHtml(item.description)}</p>${item.default_action ? `<small>Default link: ${escapeHtml(item.default_action)}</small>` : ''}</article>`).join('')}</div></section>`).join('');
  const rules = data.rules.map(rule => `<tr><td class="mono">${escapeHtml(rule.code)}</td><td><strong>${escapeHtml(rule.name)}</strong><br><small>${escapeHtml(rule.description)}</small></td><td class="mono">${escapeHtml(rule.event_type)}</td><td>${Number(rule.base_score)}</td><td>${Number(rule.threshold_count)>1 ? `${Number(rule.threshold_count)} in ${Number(rule.threshold_window_minutes)} minutes` : 'Single event'}</td><td>${levelChip(rule.risk_floor)} ${levelChip(rule.recommended_enforcement)} ${levelChip(rule.alert_severity)}</td></tr>`).join('');
  $('#viewRoot').innerHTML = `<div class="page-heading"><div><p class="eyebrow">Governed security model</p><h1>Security levels & detection rules</h1><p>Separate classifications prevent risk, authority, confidentiality and enforcement from being confused with one another.</p></div></div><div class="level-grid">${cards}</div><section class="panel" style="margin-top:16px"><div class="panel-header"><div><h2>Detection rule catalogue</h2><p>Versioned, explainable rules used by the Head Office risk engine.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Rule</th><th>Detection</th><th>Event type</th><th>Score</th><th>Threshold</th><th>Minimum response</th></tr></thead><tbody>${rules}</tbody></table></div></section>`;
}

function newSecurityEventV7Modal() {
  return modalForm('Record security or fraud event','Use this for manual reports and controlled testing. Live divisions should use the scoped event API.',{form:'v7-security-event',html:`<div class="form-grid"><label class="field"><span>Event type</span><select name="eventType" required>${['auth.failed','auth.succeeded','identity.verification_failed','payment.failed','payment.succeeded','refund.requested','chargeback.created','account.takeover_suspected','data.unauthorised_access','data.exfiltration_suspected','data.loss_reported','system.ransomware_detected','admin.security_override'].map(v=>`<option value="${v}">${v}</option>`).join('')}</select></label><label class="field"><span>Universal customer number</span><input name="customerNumber" maxlength="10" inputmode="numeric"></label><label class="field"><span>Amount</span><input name="amount" type="number" min="0" step="0.01"></label><label class="field"><span>Currency</span><input name="currency" value="GBP" maxlength="3"></label><label class="field"><span>Country code</span><input name="countryCode" maxlength="2" placeholder="GB"></label><label class="field"><span>Payment fingerprint hash</span><input name="paymentFingerprintHash" maxlength="128"></label></div><label class="check-row"><input type="checkbox" name="newDevice"> Event occurred from a newly observed device</label><label class="field"><span>Operational note</span><textarea name="note" maxlength="500"></textarea></label>`},'Process event','Risk intelligence');
}

function newIncidentV7Modal() {
  return modalForm('Open security incident','Create the central coordination record and, where relevant, start the personal-data breach assessment clock.',{form:'v7-new-incident',html:`<div class="form-grid"><label class="field"><span>Category</span><select name="category">${['cyber','fraud','account','payment','data','service','insider','physical','general'].map(v=>`<option value="${v}">${label(v)}</option>`).join('')}</select></label><label class="field"><span>Severity</span><select name="severity">${['SEV-1','SEV-2','SEV-3','SEV-4'].map(v=>`<option value="${v}">${v}</option>`).join('')}</select></label><label class="field"><span>Data classification</span><select name="dataClassification">${['D1','D2','D3','D4'].map(v=>`<option value="${v}">${v}</option>`).join('')}</select></label><label class="field"><span>Confidentiality</span><select name="confidentialityLevel">${['K0','K1','K2','K3'].map(v=>`<option value="${v}">${v}</option>`).join('')}</select></label></div><label class="field"><span>Title</span><input name="title" maxlength="180" required></label><label class="field"><span>Description and known impact</span><textarea name="description" maxlength="5000" required></textarea></label><label class="check-row"><input type="checkbox" name="possibleDataBreach"> This may involve a personal-data breach</label>`},'Open incident','Incident response');
}

function newTaskV7Modal() {
  return modalForm('Create Head Office task','Allocate a controlled action across complaints, payments, security or general customer operations.',{form:'v7-new-task',html:`<div class="form-grid"><label class="field"><span>Service area</span><select name="serviceArea">${['general','complaints','refunds','disputes','payments','security','data_protection','safeguarding'].map(v=>`<option value="${v}">${label(v)}</option>`).join('')}</select></label><label class="field"><span>Priority</span><select name="priority">${['low','normal','high','critical'].map(v=>`<option value="${v}">${label(v)}</option>`).join('')}</select></label><label class="field"><span>Universal customer number or ID</span><input name="customerId" maxlength="100"></label><label class="field"><span>Due</span><input name="dueAt" type="datetime-local"></label></div><label class="field"><span>Task title</span><input name="title" maxlength="180" required></label><label class="field"><span>Required action</span><textarea name="description" maxlength="3000" required></textarea></label>`},'Create task','Central operations');
}

function reviewAlertV7Modal(element) {
  return modalForm(`Review ${element.dataset.reference}`,'Record the triage decision, outcome and assigned owner.',{form:'v7-alert-decision',attributes:`data-id="${escapeHtml(element.dataset.id)}"`,html:`<div class="form-grid"><label class="field"><span>Status</span><select name="status">${['new','triage','investigating','actioned','false_positive','closed'].map(v=>`<option value="${v}" ${v===element.dataset.status?'selected':''}>${label(v)}</option>`).join('')}</select></label><label class="field"><span>Assign to</span><select name="assignedStaffId">${options(state.reference.staff,'id','display_name','','Unassigned')}</select></label></div><label class="field"><span>Decision</span><input name="decision" maxlength="100"></label><label class="field"><span>Reason and evidence</span><textarea name="reason" maxlength="2000" required></textarea></label>`},'Save decision','Risk alert');
}

function updateIncidentV7Modal(element) {
  return modalForm(`Update ${element.dataset.reference}`,'Record status, severity, breach assessment and the next timeline entry.',{form:'v7-incident-update',attributes:`data-id="${escapeHtml(element.dataset.id)}"`,html:`<div class="form-grid"><label class="field"><span>Status</span><select name="status">${['new','triage','contained','investigating','remediating','recovering','monitoring','closed'].map(v=>`<option value="${v}" ${v===element.dataset.status?'selected':''}>${label(v)}</option>`).join('')}</select></label><label class="field"><span>Severity</span><select name="severity">${['SEV-1','SEV-2','SEV-3','SEV-4'].map(v=>`<option value="${v}" ${v===element.dataset.severity?'selected':''}>${v}</option>`).join('')}</select></label><label class="field"><span>Data-breach position</span><select name="dataBreachStatus">${['not_assessed','not_a_breach','assessment_required','not_reportable','reportable','reported'].map(v=>`<option value="${v}" ${v===element.dataset.breach?'selected':''}>${label(v)}</option>`).join('')}</select></label><label class="field"><span>Owner</span><select name="ownerStaffId">${options(state.reference.staff,'id','display_name','','Unassigned')}</select></label></div><label class="field"><span>Timeline update</span><textarea name="timelineSummary" maxlength="1000" required></textarea></label><label class="field"><span>Breach assessment rationale</span><textarea name="breachRationale" maxlength="4000"></textarea></label><label class="check-row"><input type="checkbox" name="highRiskToRights"> High risk to individuals' rights and freedoms</label><label class="check-row"><input type="checkbox" name="reportToIco"> Report to the ICO</label><label class="check-row"><input type="checkbox" name="notifyIndividuals"> Notify affected individuals</label>`},'Record update','Incident response');
}

function updateTaskV7Modal(element) {
  return modalForm(`Update ${element.dataset.reference}`,'Record the operational status and assigned owner.',{form:'v7-task-update',attributes:`data-id="${escapeHtml(element.dataset.id)}"`,html:`<div class="form-grid"><label class="field"><span>Status</span><select name="status">${['open','in_progress','awaiting_customer','awaiting_internal','approval_required','completed','cancelled'].map(v=>`<option value="${v}" ${v===element.dataset.status?'selected':''}>${label(v)}</option>`).join('')}</select></label><label class="field"><span>Assigned to</span><select name="assignedStaffId">${options(state.reference.staff,'id','display_name','','Unassigned')}</select></label></div>`},'Save task','Central operations');
}

const baseRenderRouteV7 = renderRoute;
renderRoute = async function(route = routeFromHash()) {
  if (route === 'control-room') { state.route=route; $$('.nav-item').forEach(item=>item.classList.toggle('active',item.dataset.route===route)); $('#sidebar').classList.remove('open'); setLoading(); try{return await renderControlRoomV7();}catch(error){return showV7RouteError(error);} }
  if (route === 'risk-intelligence') { state.route=route; $$('.nav-item').forEach(item=>item.classList.toggle('active',item.dataset.route===route)); $('#sidebar').classList.remove('open'); setLoading(); try{return await renderRiskIntelligenceV7();}catch(error){return showV7RouteError(error);} }
  if (route === 'incidents-v7') { state.route=route; $$('.nav-item').forEach(item=>item.classList.toggle('active',item.dataset.route===route)); $('#sidebar').classList.remove('open'); setLoading(); try{return await renderIncidentsV7();}catch(error){return showV7RouteError(error);} }
  if (route === 'central-operations') { state.route=route; $$('.nav-item').forEach(item=>item.classList.toggle('active',item.dataset.route===route)); $('#sidebar').classList.remove('open'); setLoading(); try{return await renderCentralOperationsV7();}catch(error){return showV7RouteError(error);} }
  if (route === 'security-levels') { state.route=route; $$('.nav-item').forEach(item=>item.classList.toggle('active',item.dataset.route===route)); $('#sidebar').classList.remove('open'); setLoading(); try{return await renderSecurityLevelsV7();}catch(error){return showV7RouteError(error);} }
  return baseRenderRouteV7(route);
};

function showV7RouteError(error){$('#viewRoot').innerHTML=`<div class="panel"><div class="empty-state"><strong>The Version 7 section could not be opened</strong><span>${escapeHtml(error.message)}</span></div></div>`;toast('Section unavailable',error.message,'error');}

const baseHandleFormV7 = handleForm;
handleForm = async function(form) {
  const name=form.dataset.form;
  if (!name || !name.startsWith('v7-')) return baseHandleFormV7(form);
  const data=Object.fromEntries(new FormData(form));
  const errorElement=$('.form-error',form);if(errorElement)errorElement.textContent='';
  const submit=$('button[type="submit"],button:not([type])',form);if(submit)submit.disabled=true;
  try{
    if(name==='v7-security-event'){if(data.amount){data.amountMinor=Math.round(Number(data.amount)*100);}delete data.amount;data.attributes={newDevice:Boolean($('[name="newDevice"]',form)?.checked),note:data.note||''};delete data.note;delete data.newDevice;const result=await api('/api/v7/events',{method:'POST',body:JSON.stringify(data)});closeModal();toast('Security event processed',`${result.event.riskLevel} · score ${result.event.score}`);return renderRoute();}
    if(name==='v7-new-incident'){data.possibleDataBreach=Boolean($('[name="possibleDataBreach"]',form)?.checked);const result=await api('/api/v7/incidents',{method:'POST',body:JSON.stringify(data)});closeModal();toast('Incident opened',result.reference);return renderRoute();}
    if(name==='v7-new-task'){const result=await api('/api/v7/operations',{method:'POST',body:JSON.stringify(data)});closeModal();toast('Head Office task created',result.reference);return renderRoute();}
    if(name==='v7-alert-decision'){data.id=form.dataset.id;await api('/api/v7/alerts',{method:'PUT',body:JSON.stringify(data)});closeModal();toast('Alert decision recorded');return renderRoute();}
    if(name==='v7-incident-update'){data.id=form.dataset.id;data.highRiskToRights=Boolean($('[name="highRiskToRights"]',form)?.checked);data.reportToIco=Boolean($('[name="reportToIco"]',form)?.checked);data.notifyIndividuals=Boolean($('[name="notifyIndividuals"]',form)?.checked);await api('/api/v7/incidents',{method:'PUT',body:JSON.stringify(data)});closeModal();toast('Incident updated');return renderRoute();}
    if(name==='v7-task-update'){data.id=form.dataset.id;await api('/api/v7/operations',{method:'PUT',body:JSON.stringify(data)});closeModal();toast('Task updated');return renderRoute();}
  }catch(error){if(errorElement)errorElement.textContent=error.message;else toast('Action could not be completed',error.message,'error');}finally{if(submit)submit.disabled=false;}
};

const baseHandleClickV7 = handleClick;
handleClick = async function(target) {
  const element=target.closest('[data-action]');
  const action=element?.dataset.action;
  if(action==='new-security-event')return newSecurityEventV7Modal();
  if(action==='new-v7-incident')return newIncidentV7Modal();
  if(action==='new-v7-task')return newTaskV7Modal();
  if(action==='review-v7-alert')return reviewAlertV7Modal(element);
  if(action==='update-v7-incident')return updateIncidentV7Modal(element);
  if(action==='update-v7-task')return updateTaskV7Modal(element);
  return baseHandleClickV7(target);
};
